{
  "name": "typesafe-php-api-generator",
  "version": "1.0.0",
  "description": "A typesafe Scaffolder wich generates a PHP application based on a given data model.",
  "main": "index.js",
  "scripts": {
    "generate": "tsc && node dist/generator.js",
    "start": "node dist/generator.js",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven K",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^9.0.1",
    "immutable": "^4.0.0-rc.12",
    "path": "^0.12.7",
    "typescript": "^4.0.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.5",
    "@types/node": "^14.14.14"
  }
}
